import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.ytimg.com', "cdn4.iconfinder.com"], // Add your image hostnames here
  },
};

export default nextConfig;
